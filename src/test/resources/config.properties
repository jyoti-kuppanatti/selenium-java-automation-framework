# Base/default configuration. Values here are overridden by env-specific
# files (e.g. config-qa.properties) and by -D system properties.

base.url=https://opensource-demo.orangehrmlive.com/web/index.php/auth/login

browser=chrome
headless=false

implicit.wait.seconds=5
explicit.wait.seconds=15
pageload.timeout.seconds=30

# Placeholder credentials - overridden per-environment or via env vars.
# See .env.example for the recommended way to supply real values.
valid.username=Admin
valid.password=admin123
